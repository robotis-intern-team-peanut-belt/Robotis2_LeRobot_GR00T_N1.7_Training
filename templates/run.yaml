# Run override template; shared defaults stay in configs/base/groot_n17.yaml.
_base: ../base/groot_n17.yaml
name: TODO_unique_run_name
description: "REPLACE ME: describe this run's purpose and deliberate changes."
hypothesis: "REPLACE ME: state a falsifiable expectation and reason."
dataset_ref: TODO_dataset_name
# Add only deliberate experiment overrides below.
# policy:
#   use_relative_actions: false
# train:
#   seed: 42
annotation:
  owner: intern-team
  notes: "REPLACE ME: add dataset-specific evaluation criteria, caveats, and intended comparisons."
